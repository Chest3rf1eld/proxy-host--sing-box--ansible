all:
  children:
    sing_box:
      hosts:
        sing-box-01:
          ansible_host: 203.0.113.10
          ansible_user: root
          ansible_ssh_private_key_file: ~/.ssh/id_ed25519
          # Set to false for offline hosts where sing-box is already installed.
          sing_box_install_package: true
